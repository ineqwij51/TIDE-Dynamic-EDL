# Third-party boundary

This directory intentionally contains no external source or pretrained weight. Install pinned official baseline repositories under the ignored `third_party/sources/` directory and comply with each upstream license. See `configs/baselines.yaml` and `docs/baselines.md`.

